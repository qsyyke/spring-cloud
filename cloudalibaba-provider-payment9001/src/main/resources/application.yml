server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.75.128:8848 #配置Nacos地址 这里不能加上http

management:
  endpoints:
    web:
      exposure:
        include: '*'

